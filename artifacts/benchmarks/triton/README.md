# Triton benchmark artifacts

Status: **NOT RUN** on the current CPU-only Apple Silicon environment.

On an NVIDIA host, `benchmarks/triton/run_matrix.sh` writes raw Performance
Analyzer CSV files and `summary.json` here. Do not add hand-edited measurements;
every reported value must be traceable to those raw files and an environment
record.
